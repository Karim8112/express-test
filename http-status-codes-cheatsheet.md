# الدليل البرمجي الشامل لرموز حالة HTTP في تصميم RESTful APIs
> **مرجع سريع لمهندسي الـ Backend ومطوري Node.js / Express**

عند بناء واجهات برمجة التطبيقات (RESTful APIs)، لا نكتفي بإرسال البيانات فحسب، بل نرسل معها **رموز حالة HTTP (HTTP Status Codes)** لتوضيح طبيعة النتيجة بدقة للـ Frontend أو الأنظمة الأخرى. هذا الدليل تم تصميمه ليكون مرجعاً يومياً مخزناً لديك.

---

## 🟢 1. عائلة النجاح (2xx Success) - "كل شيء تم بنجاح"

تُستخدم عندما يستقبل السيرفر الطلب ويعالجه بنجاح دون أخطاء.

### `200 OK`
* **المعنى:** نجاح عام ومعالجة سليمة للطلب.
* **متى تستخدمه؟**
  * مع طلبات **GET** لجلب البيانات (مثل جلب قائمة المنتجات أو الملف الشخصي).
  * مع طلبات **PUT** أو **PATCH** بعد نجاح تحديث المورد بنجاح وإعادة الكائن المحدث.
* **مثال في Express:**
  ```javascript
  res.status(200).json({ status: 'success', data: { products } });
  ```

### `201 Created`
* **المعنى:** تم استقبال الطلب بنجاح وتم **إنشاء مورد جديد وحفظه** في قاعدة البيانات.
* **متى تستخدمه؟**
  * حصرياً مع طلبات **POST** عند إضافة عنصر جديد (مثل إنشاء حساب مستخدم، أو إضافة منتج جديد).
* **مثال في Express:**
  ```javascript
  res.status(201).json({ status: 'success', data: { newProduct } });
  ```

### `204 No Content`
* **المعنى:** تم تنفيذ العملية بنجاح تام، ولكن **لا توجد أي بيانات لإرجاعها** في جسم الاستجابة (Response Body).
* **متى تستخدمه؟**
  * مع طلبات **DELETE** بعد حذف العنصر بنجاح؛ حيث لا يحتاج العميل لرؤية كائن محذوف.
* **مثال في Express:**
  ```javascript
  res.status(204).send(); // تذكر عدم إرسال أي JSON أو نصوص هنا
  ```

---

## 🟡 2. عائلة إعادة التوجيه (3xx Redirection) - "المورد ليس هنا"

تُستخدم لإعلام العميل بأن المورد الذي يطلبه قد تم نقله إلى رابط آخر. (نادرة الاستخدام في واجهات الـ API الصرفة وتظهر بكثرة في تصفح الويب).

### `301 Moved Permanently`
* **المعنى:** تم نقل الرابط بشكل دائم إلى عنوان جديد. يقوم المتصفح تلقائياً بالتحويل للرابط الجديد وتحديث محركات البحث.

### `304 Not Modified` (هام جداً للأداء)
* **المعنى:** لم يطرأ أي تغيير على البيانات منذ آخر مرة طلبها العميل.
* **متى تستخدمه؟**
  * عند تفعيل الكاش (Caching). يطلب العميل البيانات، فيرى السيرفر أنها لم تتغير، فيرسل `304` بدون بيانات؛ مما يجعل العميل يقرأ البيانات من ذاكرة جهازه ويوفر استهلاك شبكة الإنترنت وسرعة خارقة.

---

## 🔴 3. عائلة أخطاء العميل (4xx Client Errors) - "المشكلة في طلبك أنت"

تُستخدم عندما يرسل العميل طلباً خاطئاً، أو يفتقد للصلاحيات، أو يطلب شيئاً غير موجود. **هنا يتحمل العميل المسؤولية.**

### `400 Bad Request`
* **المعنى:** الطلب غير مفهوم أو يفتقد لبيانات حاسمة لتنفيذه.
* **متى تستخدمه؟**
  * عند فشل التحقق من المدخلات (Validation Failure)؛ مثلاً أرسل العميل طلب POST لإضافة منتج ولكنه نسي إدخال حقل "السعر" أو كتبه كـ "نص" بدلاً من "رقم".
* **مثال في Express:**
  ```javascript
  if (!req.body.price) {
    return res.status(400).json({ status: 'fail', message: 'السعر حقل مطلوب!' });
  }
  ```

### `401 Unauthorized`
* **المعنى:** العميل غير معروف أو لم يقم بتسجيل الدخول بعد.
* **متى تستخدمه؟**
  * عندما يطلب العميل صفحة محمية (مثل لوحة التحكم) دون إرسال رمز التحقق (JWT Token) في ترويسة الطلب، أو إذا كان الرمز منتهي الصلاحية.

### `403 Forbidden`
* **المعنى:** العميل معروف ومسجل دخول، ولكنه **لا يمتلك الصلاحيات الكافية** لتنفيذ هذا الإجراء.
* **متى تستخدمه؟**
  * عندما يحاول مستخدم برتبة "عميل" حذف منتج، وهو إجراء مسموح به فقط لرتبة "مدير (Admin)".
* **مثال في Express:**
  ```javascript
  if (req.user.role !== 'admin') {
    return res.status(403).json({ status: 'fail', message: 'غير مسموح لك بالقيام بهذا الإجراء!' });
  }
  ```

### `404 Not Found`
* **المعنى:** المورد الذي تطلبه غير موجود على الخادم على الإطلاق.
* **متى تستخدمه؟**
  * إذا طلب العميل رابطاً غير معرف في السيرفر (مثل `/api/v1/invalid-route`).
  * إذا طلب العميل منتجاً برقم معرف (ID) لا وجود له في قاعدة البيانات (مثل `/api/v1/products/999999`).
* **مثال في Express:**
  ```javascript
  const product = await Product.findById(req.params.id);
  if (!product) {
    return res.status(404).json({ status: 'fail', message: 'لا يوجد منتج بهذا الرقم المعرف!' });
  }
  ```

---

## 🔥 4. عائلة أخطاء السيرفر (5xx Server Errors) - "المشكلة في كودي أنا"

تُستخدم عندما يكون العميل قد أرسل طلباً سليماً تماماً، ولكن السيرفر فشل في معالجته بسبب عطل داخلي أو انهيار في الكود. **هنا يتحمل المبرمج المسؤولية.**

### `500 Internal Server Error`
* **المعنى:** حدث خطأ غير متوقع بداخل الكود البرمجي للسيرفر أو تعطل الاتصال بقاعدة البيانات.
* **متى تستخدمه؟**
  * عند الإمساك بـ Exception غير متوقع في كتل الـ `catch`، أو انهيار السيرفر أثناء تنفيذ دالة برمجية.
* **مثال في Express:**
  ```javascript
  } catch (err) {
    return res.status(500).json({ status: 'error', message: 'حدث خطأ داخلي في السيرفر، جاري العمل على حله.' });
  }
  ```

### `502 Bad Gateway`
* **المعنى:** السيرفر يعمل كوسيط (Proxy) واستقبل استجابة خاطئة من سيرفر خلفي (مثل فشل NGINX في التحدث مع تطبيق Node.js الخاص بك).

### `503 Service Unavailable`
* **المعنى:** السيرفر عاجز عن معالجة الطلبات حالياً بسبب الصيانة الدورية أو الحمل الزائد الهائل على المعالج.

### `504 Gateway Timeout`
* **المعنى:** انتهت المهلة الزمنية المحددة للسيرفر الوسيط بانتظار الرد من السيرفر الداخلي.

---

## 🛡️ 5. القواعد الذهبية لتصميم استجابات RESTful احترافية

لتجعل واجهة الـ API الخاصة بك فخمة ومطابقة لمعايير الشركات الكبرى، التزم بهذين القانونين:

### القانون الأول: لا تستخدم رمز `200` للأخطاء أبداً!
بعض المطورين يقعون في فخ فادح؛ حيث يقومون بإرجاع حالة `200 OK` للمتصفح، وبداخل الكائن يكتبون رسالة خطأ:
```json
// ❌ خطأ تصميمي فادح وكارثة برمجية!
HTTP Status: 200 OK
{
  "success": false,
  "error": "المستخدم غير موجود!"
}
```
**السبب المعماري للتجنب:** يمنع هذا التصميم العميل (مثل مكتبة Axios أو Fetch في الـ Frontend) من إلقاء خطأ تلقائي في بلوك الـ `catch`؛ مما يضطر مبرمج الواجهة لكتابة مئات الشروط يدوياً للتأكد من عدم وجود رسائل خطأ بالداخل. التزم دائماً بالرمز المناسب للحدث.

### القانون الثاني: التزم بمعيار JSend في صياغة الاستجابة
قسّم استجاباتك دائماً لثلاث فئات واضحة بناءً على الرمز:

1. **النجاح (`status: "success"`):** للرموز من 200 إلى 299.
2. **فشل العميل (`status: "fail"`):** للرموز من 400 إلى 499 (البيانات المدخلة خاطئة أو ناقصة).
3. **خطأ السيرفر (`status: "error"`):** للرموز من 500 إلى 599 (مشكلة أمنية أو برمجية في الكود).
